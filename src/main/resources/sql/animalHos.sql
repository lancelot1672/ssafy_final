create table animalhos (
	no int auto_increment primary key,
    name varchar(30) not null,
    street_addr varchar(100),
    site_addr varchar(100),
    lat varchar(20) not null,
    lng varchar(20) not null
);